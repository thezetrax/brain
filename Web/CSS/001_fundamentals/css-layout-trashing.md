# Layout Trashing

- **Layout trashing** is a performance issue
- Occurs when the browser has to recalculate the layout of a webpage multiple times in quick succession
- This can happen when 
  - JavaScript code modifies the DOM (Document Object Model)
  - CSS properties that affect the layout, such as width, height, margin, padding, etc.
